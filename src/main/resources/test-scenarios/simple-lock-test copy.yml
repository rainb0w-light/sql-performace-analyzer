scenario:
  name: "测试场景"
  datasource: "mysqlDataSource"
  defaultIsolationLevel: "READ_COMMITTED"
  threads:
    thread1:
      steps:
        - id: "step1"
          sqls: ["insert into users (id, username,email,password_hash) values (1, 'test','alice@example.com','hash1')"]
        - id: "step2"
          sqls: ["select * from users  where id = 1 for update"]
    thread2:
      steps:
        - id: "step1"
          sqls: ["select * from users  where id = 1 for update"]
        - id: "step2"
          sqls: ["UPDATE users SET username = 'test' where id = 100"]