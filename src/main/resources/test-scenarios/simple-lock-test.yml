scenario:
  name: "测试场景"
  datasource: "mysqlDataSource"
  defaultIsolationLevel: "READ_COMMITTED"
  threads:
    thread1:
      steps:
        - id: "step1"
          sqls: ["select * from  users where id = 1 for update"]
    thread2:
      steps:
        - id: "step1"
          sqls: ["UPDATE users SET username = 'test' where id = 1", "COMMIT"]